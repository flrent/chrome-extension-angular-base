/* global describe, it */

(function () {
    'use strict';

    afterEach(function() {

    });

    describe('when this condition is matched', function() {
        it('should do good', function() {
            expect(1).to.equal(1);
        })
    });
})();
